# Running the project

This repository is a static HTML page. The Replit preview is served by the
dependency-free Perl server:

```bash
perl server.pl
```

It listens on `0.0.0.0:5000` and serves `index.html` at `/`.